
fullnameOverride: "cnpg-db-operator"

crds:
  create: true

config:
  create: true
  # -- The name of the configmap/secret to use.
  name: cnpg-controller-manager-config

  grafanaDashboard:
    create: true