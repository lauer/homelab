apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: security
  namespace: argocd
spec:
  syncPolicy:
    preserveResourcesOnDeletion: true
  generators:
    - git:
        repoURL: https://github.com/lauer/homelab
        revision: play
        directories:
          - path: k8s/services/security/*
  template:
    metadata:
      name: '{{ path.basename }}'
      labels:
        homeops.services.component: security
    spec:
      project: default
      source:
        repoURL: https://github.com/lauer/homelab
        targetRevision: play
        path: '{{ path }}'
      destination:
        name: in-cluster
        namespace: '{{ path.basename }}'
      syncPolicy:
        automated:
          prune: true